# Brand assets

The site loads these files directly:

- `logo.png` — the star-Z mark (navbar, hero, footer, loading screen).
- `wordmark.png` — the jagged ZOKIX wordmark banner (navbar, hero, footer,
  loading screen).

Both currently contain generated placeholders. **Overwrite them with the
original brand files (keep the same file names)** and the whole site will
show the exact artwork — no code changes needed. Transparent-background
images look best.
